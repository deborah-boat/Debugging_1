// Easy

const car = {
brand: "Volvo",
model: "XC60", // Missed a komma
year: 2020,
color: "brown" // missed color value
};
console.log(car.color);

//

const point = { x: 10, y: 20 }; // square bracket to curly braces
console.log(point.x);

//

const user = {
name: "Mila",
email: "mila@mail.com" // mispelled email
};

console.log(user.email);

//

const profile = { contact: { email: "a@b.com" } };
console.log(profile.contact.email); // not profile.contact